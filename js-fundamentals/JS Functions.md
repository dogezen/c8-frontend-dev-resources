```js

// optionally, we might have parameters (aka arguments)
// these are values/data/info we pass to the function
// because the function needs that information to perform its action
function functionName(parameter1, parameter2) {
  // block of code that is run when the function is CALLED using functionName()
  // optional, we might return a result (generate an output)
  return result; // result can be anything: value, a jsObject, an array...
}

```